/*// create express router
const express = require('express');
const router = express.Routher(); // create router

router.get('/', (req, res) => {
	res.json({
		message: 'lock'
	});
});

module.exports = router; // makes router availabe*/